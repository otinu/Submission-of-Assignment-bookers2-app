

<h3>Books</h3>
<table>
  <thead>
    <tr>
     <th colspan="3"></th>
     <th>Title</th>
     <th>Opnion</th>
    </tr>
  </thead>

  <tbody>
   <% @book.each do |f| %>

   <tr>
    <td><%= attachment_image_tag f.user, :profile_image, :fill, 30, 30, fallback: "no_image.jpg" %></td>
    <td><%= f.title %></td>
    <td><%= f.body %></td>
  </tr>

   <% end %>
  </tbody>
</table>

<%= render 'users/mypage-edit', user: @user %>
<%= render 'books/newbook', book: Book %>










